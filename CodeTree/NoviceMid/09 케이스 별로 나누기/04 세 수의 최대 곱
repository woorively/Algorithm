# import sys

n = int(input())
nums = list(map(int, input().split()))
nums.sort()
# ans = -sys.maxsize

# if nums[0] < 0 and nums[1] < 0:
#     ans = max(ans, nums[0] * nums[1] * nums[-1])

ans = max(nums[0] * nums[1] * nums[-1], nums[-3] * nums[-2] * nums[-1])

print(ans)
